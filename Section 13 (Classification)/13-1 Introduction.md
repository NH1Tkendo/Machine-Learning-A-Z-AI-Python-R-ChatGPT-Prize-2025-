## Giới thiệu Phần 3: Phân loại (Classification)

## Khái niệm cơ bản

Khác với **Hồi quy** `(Regression)` dùng để dự đoán một con số liên tục, **Phân loại** `(Classification)` được sử dụng để dự đoán một nhóm hoặc danh mục `(category)`.

- **Ứng dụng**: Rất đa dạng, trải dài từ y học đến marketing.
    

## Các mô hình Phân loại

Trong phần này, nội dung sẽ tập trung vào việc tìm hiểu và triển khai các mô hình Machine Learning sau:

**1. Các mô hình tuyến tính (Linear models):**

- Hồi quy Logistic `(Logistic Regression)`
    
- Máy vector hỗ trợ `(Support Vector Machine - SVM)`
    

**2. Các mô hình phi tuyến tính (Nonlinear models):**

- K-Láng giềng gần nhất `(K-Nearest Neighbors - K-NN)`
    
- Kernel SVM
    
- Phân loại Rừng ngẫu nhiên `(Random Forest Classification)`
    
- Phân loại Cây quyết định `(Decision Tree Classification)`
    
- Naive Bayes
    

👉 **Mục tiêu**: Hiểu rõ lý thuyết và cách triển khai code cho từng loại mô hình trên để giải quyết các bài toán phân loại thực tế.